#!/usr/bin/env zsh
set -euo pipefail

ROOT_DIR="$(cd "$(dirname "$0")" && pwd)"
LOG_FILE="$ROOT_DIR/.slidebuilder.log"

if [[ ! -f "$LOG_FILE" ]]; then
  echo "No log file yet at $LOG_FILE"
  exit 0
fi

tail -f "$LOG_FILE"
